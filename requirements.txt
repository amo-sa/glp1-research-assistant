# Core dependencies for glp1-research-assistant
# Direct imports only -- transitive dependencies resolved by pip at install time

# Data ingestion (Step 1)
requests==2.34.2
python-dotenv==1.2.2

# Embeddings and vector store (Step 2)
sentence-transformers==5.6.0
chromadb==1.5.9

# LLM client (Steps 3, 4)
openai==2.44.0

# Multi-agent pipeline (Step 4)
langgraph==1.2.6

# Evaluation harness (Step 5)
ragas==0.3.9
langchain-core==1.4.8
langchain-openai==1.3.3
langchain-community==0.4.2
langchain-huggingface==1.2.2

# Tracing and observability (Step 6)
arize-phoenix==17.12.0
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1
openinference-instrumentation-langchain==0.1.66

# From-scratch attention component (Step 7)
numpy==2.4.6
transformers==5.12.1
torch==2.12.1